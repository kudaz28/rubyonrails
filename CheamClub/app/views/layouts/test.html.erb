<section class="sidebar" id="right">
<% if member_signed_in? %>
  Signed in as <%= current_member.email %>:
  <%= link_to "Sign Out", destroy_member_session_path, :method => :delete %> 
<% else %>
  <%= link_to "Sign Up", new_member_registration_path %> or <%= link_to "Sign In",
  new_member